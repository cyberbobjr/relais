# RELAIS Environment Variables Template
LOG_LEVEL=INFO

OPENROUTER_API_KEY=sk-or-xxx

# Messaging Bridges
DISCORD_BOT_TOKEN=xxx
TELEGRAM_BOT_TOKEN=xxx
SLACK_BOT_TOKEN=xoxb-xxx
SLACK_SIGNING_SECRET=xxx

# Redis Main Configuration
# Default project-local path: ./.relais/redis.sock
REDIS_SOCKET_PATH=./.relais/redis.sock
REDIS_PASSWORD=xxx

# Redis Bricks Passwords (ACLs)
REDIS_PASS_AIGUILLEUR=xxx
REDIS_PASS_PORTAIL=xxx
REDIS_PASS_SENTINELLE=xxx
REDIS_PASS_ATELIER=xxx
REDIS_PASS_SOUVENIR=xxx
REDIS_PASS_ARCHIVISTE=xxx
REDIS_PASS_COMMANDANT=xxx
REDIS_PASS_FORGERON=xxx

ANTHROPIC_API_KEY=sk-ant-xxx

# WhatsApp adapter (Baileys gateway — fazer-ai/baileys-api)
# Required when whatsapp.enabled: true in aiguilleur.yaml
WHATSAPP_GATEWAY_URL=http://localhost:3025
WHATSAPP_PHONE_NUMBER=+33612345678        # bot's phone number in E.164 format
WHATSAPP_API_KEY=                          # generated via: bun scripts/manage-api-keys.ts create user relais
WHATSAPP_WEBHOOK_SECRET=your-webhook-secret
WHATSAPP_WEBHOOK_PORT=8765
WHATSAPP_WEBHOOK_HOST=127.0.0.1           # also used as callback URL for gateway (same-host only)

# Redis password for baileys-api (TCP connection)
REDIS_PASS_BAILEYS=pass_baileys

# Optional overrides
# Répertoire de données RELAIS (config, skills, logs, redis.sock…).
# Par défaut : .relais/ à la racine du projet.
# Pour un déploiement système, pointer vers un chemin absolu :
RELAIS_HOME=./.relais
